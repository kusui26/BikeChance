# Week 2 実装プラン — アーカイブと配信の土台

## 0. この文書について

- **目的**：`docs/260904_dev_plan.md`（開発プラン）の §13 で W2 に置いた作業を、「何を・どの順で・どの単位の PR で作るか」まで落とす。W1 の `docs/260905_week1_data_collection.md` と同じ役割の文書で、W2 が終わったら実測と所見を追記して閉じる。
- **前提**：W1 は 2026-09-06〜09-07 に完了し、収集が毎分で本番稼働している（M0 9/6 20:07 JST、M1 9/7 20:56 JST）。当初計画より 4 日早く、カレンダーに 1 週間の余裕がある。
- **期間**：2026-09-08（月）〜09-12（金）。開発プランの W2 は 9/14 開始だったが、前倒しできる。以降の週の期日は動かさない。
- **参照の表記**：本文書内の章節は `§4.2` のように書く。上位文書を指す場合は「開発プラン §6.7」のように前置きし、W1 の文書は「W1 プラン §5.7」と書く。
- **この文書の読み方**：§3 に W2 で確定させた設計判断（W2-01〜W2-13）を、§4 に踏みそうな落とし穴を、§5 に **8 本の PR** を実行順に定義する。§9 の付録に、PR をまたぐ契約（アーカイブのパス規約・Parquet のスキーマ・再構築の手順）を置く。
- **数値の出どころ**：§10 に実測ログを置いた。本文の数値はすべてそこか、W1 プラン・開発プランの実測に対応する。推定値には「見込み」と書く。

## 1. Week 2 のゴールと完了条件

**ゴール：集めたデータを Postgres の外でも永続化し、最初の読み出し経路を開く。**

W1 で「集める」は完成した。W2 の主題は 2 つある。ひとつは**取り返しのつかない損失を止めること**。もうひとつは**貯めたものを DB の寿命から切り離すこと**である。どちらも締切がある。

| マイルストーン | 目標日 | 完了条件 |
|---|---|---|
| **M2− 通知の到達** | 9/8（月）午前 | 監視の通知先を Vault に登録し、強制発火で実際に届くことを確認する |
| **M2− 天気の保全開始** | 9/8（月） | 予報の生 JSON が毎時 Storage に貯まり始める。**遅れた日数はそのまま失われるため、W2 で最初に行う** |
| **再構築の実証** | 9/9（火） | 生 JSON から `status_snapshots` を再構築し、PR 0 期の積み残し（158 オブジェクト）を取り込んで §7.3 の照合が通る |
| **M2 Parquet アーカイブ開始** | 9/10（水） | 毎時ジョブが前 1 時間分を Parquet で Storage に書き、2 回目の実行で同じ時間帯を上書きしても内容が変わらない |
| **現在値の配信** | 9/11（木） | `/v1/meta` が実際の鮮度を返し、`/v1/stations` が bbox 指定で現在値を返す |
| **W2 完了** | 9/12（金） | 下表をすべて満たす |

**W2 の合格基準**

| 指標 | 期待値 | 合格ライン |
|---|---|---|
| 収集の継続 | W1 と同じ | **中断 0**（`feed_fetch_log` の呼び出し間隔の最大 < 180 秒） |
| 監視の通知 | 届く | 強制発火が実際に Webhook に届く |
| 天気アーカイブの取得率 | 24 回/日 | **≥ 23 回/日**（1 回の失敗は許容。次の時間に取り直せる） |
| 再構築の一致 | 生 JSON と一致 | PR 0 期の全オブジェクトを取り込み、**照合の不一致 0** |
| Parquet の冪等性 | 2 回実行で同一 | **バイト単位で同一、または行の集合が同一** |
| Parquet の容量 | 見込み 5〜10 MB/日 | **≤ 20 MB/日** |
| `/v1/stations` の応答 | — | p75 < 500 ms（東京から、CDN 未ヒット時） |
| DB サイズ | 22.2 MB/日（W1 実測） | **≤ 40 MB/日**（W2 で増える要素は無いはず） |

**W2 で意図的に測らないもの**：モデルの精度。W2 時点のデータは 5〜6 日分しかなく、開発プラン §6.6 の「14 日でベースライン」に届かない。EDA は分布の把握までにとどめる。

## 2. 着手時点の状態（2026-09-07 23:00 JST）

**動いているもの**

| 項目 | 状態 |
|---|---|
| 収集 | 毎分 × 2 システム。累計 1,551 スナップショット（HELLO 341・ドコモ 1,210）。**ジョブ失敗 0・取得エラー 0・異常スナップショット 0** |
| 生データ | `gbfs-raw` に status 1,709 個 / 87 MB、information 4 個 / 2.1 MB |
| DB | 60 MB。マイグレーション 0001〜0014 適用済み。RLS は 14 テーブル全部で有効 |
| 属性同期 | 日次 04:00 JST。有効行 HELLO 14,922・ドコモ 5,820 |
| 監視 | `monitor_feeds` 5 分毎、`watchdog_collect` 毎分。**誤報 0**。日次ジョブ 3 本 |
| 公開 API | `/v1/meta` のみ。**DB に未接続で `stale: true` を返す** |

**W2 で使う土台のうち、まだ無いもの**

| 項目 | 現状 | どの PR で作るか |
|---|---|---|
| 監視の通知先 | Vault に `cron_secret` のみ。`alert_webhook_url` が無く、**検知しても誰にも届かない** | 段 0（PR なし・運用作業） |
| 天気の取得 | 実装なし | PR A |
| 生 JSON からの再構築 | **スクリプトが無い**（CLAUDE.md §6 が求めている） | PR B |
| `apps/ml` | ディレクトリごと無い。`vercel.json` の `services` は `web` だけ | PR C |
| Parquet | 無い | PR D |
| 匿名ロールの読み取り | **SELECT 権限が 1 つも無く、RLS ポリシーも 0 件** | PR E |
| データ辞書 | 無い（W1 の手順 9 が持ち越し） | PR F |
| TypeScript のスクリプト実行系 | `tsx` / `vite-node` / `esbuild` いずれも未導入 | PR B で判断（W2-04） |

**積み残し**：PR 0 期（2026-09-06 06:51〜09:40 UTC）の生 JSON が **158 オブジェクト**（ドコモ 124・HELLO 34）、DB に対応する行を持たない。PR D（DB 取り込み）より前の期間だから正常だが、**この 3 時間弱は今の DB からは見えない**。PR B の題材にする。

## 3. W2 で確定させた設計判断

W1 の W1-01〜W1-31 に続けて W2-01 から採番する。**実装前に決め、実装で変わったら §3 と開発プランを直してから進む**（CLAUDE.md §2 の原則 10）。

### 3.1 天気の保全について

| ID | 論点 | 決定 | 根拠 |
|---|---|---|---|
| **W2-01** | 天気を W2 に前倒しするか | **予報の生 JSON を毎時アーカイブする作業だけを W2 に置く。解釈（テーブル化・特徴量）は W4 のまま** | 天気は「`t` 時点で入手できた予報」を使う決まりで、**過去に遡って観測できない唯一の入力**（開発プラン §6.7）。ODPT のデータは生 JSON からいくらでも作り直せるが、これだけは違う。保存だけなら 1 日で作れるので、解釈を待つ理由が無い |
| **W2-02** | 何を保存するか | **API の応答をそのまま gzip して Storage に置く。パースも変換もしない** | PR 0 と同じ思想（開発プラン D-03）。W4 で特徴量の定義が変わっても、保存済みのアーカイブから作り直せる。テーブル設計を先に決める必要が無いぶん、着手が速い |
| **W2-03** | 地点の粒度 | **jma_msm の格子 595 セルをすべて取る。クラスタリングしない** | 開発プランは「約 50 クラスタ」としていたが、実測ではポートは **595 格子**に分布する（HELLO 463・ドコモ 218、重なり 86）。Open-Meteo は**複数地点を 1 回の要求でまとめられる**ので、100 地点 × 6 要求で全部取れる（実測 2.0 秒 / 214 KB / 100 地点）。1 日 144 要求で、無料枠 10,000/日 の 1.4%。クラスタリングして空間解像度を落とす理由が無い |
| **W2-04** | 予報の「発行時点」 | **ライブのアーカイブを今日から始める。過去分は Historical Forecast API で埋め、どの近似を使ったかを記録する** | 学習で使うのは「`t` 時点で入手できた予報」で、これは発行時点に依存する。実測（東京駅・2026-08・744 時間）で、**最新の予報と 1 日前の予報は降雨フラグが 15% 食い違う**（MAE 0.272 mm/h）。過去 API の既定（リード 0〜24 時間）は水平 3 時間の学習には**新しすぎて漏洩し得る**。`previous_day1` は古すぎる。**厳密に正しいものはライブでしか取れない** |

### 3.2 再構築について

| ID | 論点 | 決定 | 根拠 |
|---|---|---|---|
| **W2-05** | 再構築の実装言語 | **TypeScript で書き、`packages/gbfs-core` をそのまま再利用する**（開発プラン §5.6 が挙げていた Python 実装は採らない） | 正規化の規則（負値の丸め・重複排除・フラグ・経過秒）は `gbfs-core` に 69 件のテストつきで実装済み。Python で書き直すと**本番と再構築で正規化がずれる**（開発プラン R7 と同じ型のリスク）。再構築は「本番と同じ手続きをやり直す」ことなので、同じ実装を通すべき |
| **W2-06** | TS スクリプトの実行手段 | **`tsx` を devDependency として 1 つ追加する。ユーザーの承認を得てから** | `gbfs-core` は `exports` が `./src/index.ts` で、内部の import に拡張子が無い。Node 22 の `--experimental-strip-types` では解決できないことを実測で確認した。`vite-node` も `esbuild` も入っていない。**W2-05 を成立させるには実行系が要る**。dev 専用で本番には出ない |
| **W2-07** | 再構築が呼ぶ RPC | **`ingest_snapshot` だけを呼ぶ。`begin_fetch` と `finish_fetch` は呼ばない** | 後者 2 つは `feed_state`（claim・ETag・連続失敗・最終観測）を書く。過去のデータを流し込むときにこれを触ると、**現在の収集の状態が壊れる**。`ingest_snapshot` は `status_snapshots` と `stations`、`station_status_latest` にしか触らず、後退したスナップショットでは最新状態を書かない（W1 プラン §5.3 の 34） |
| **W2-08** | 過去月のパーティション | **再構築の前に対象月のパーティションが在ることを確かめる。無ければ作る手段を用意する** | `ensure_snapshot_partitions` は**今月から先しか作らない**（実装で確認）。過去月を再構築すると行は DEFAULT パーティションに落ち、`monitor_feeds` が通知を上げる。W2 で扱う 9 月は現行月なので問題は出ないが、**本当の障害復旧では必ず踏む** |

### 3.3 Parquet について

| ID | 論点 | 決定 | 根拠 |
|---|---|---|---|
| **W2-09** | 圧縮ジョブの入力 | **Postgres の `status_snapshots` から作る**（開発プラン §5.6 の「生 JSON を読む」から変更） | 生 JSON から作ると、正規化を Python で書き直すことになり W2-05 と矛盾する。Postgres の配列は検証・重複排除・`idx` の整列が済んでいる。**「生 JSON → Postgres」は再構築（PR B）の担当、「Postgres → Parquet」は圧縮の担当**と分けると、それぞれ 1 つの関心事に閉じる。Postgres の正しさは `reconcile-snapshot.mjs` が生 JSON と独立に検証している |
| **W2-10** | Parquet のキー | **`station_id` を持たせる。`idx` は書かない** | `idx` は台帳の採番順に依存し、**全面再構築すると変わり得る**。過去に書いた Parquet と将来の台帳で `idx` の意味がずれると、静かに壊れる。`station_id` は提供側の識別子で不変 |
| **W2-11** | Parquet の形式 | **長形式**（1 行 = 1 ポート × 1 観測）。`station_id, observed_at` でソート、zstd、`date=YYYY-MM-DD/hour=HH` で分割 | 学習はポート単位の時系列を読む。配列形式は書き込みに最適化されていて、ポート単位の抽出に全行の展開が要る（開発プラン §6.7）。長形式にすれば列指向の恩恵が効く。見込みサイズは 1 日 1,050 万行 × 0.3〜0.7 B/行 ＝ **5〜10 MB/日** |

### 3.4 配信と体制について

| ID | 論点 | 決定 | 根拠 |
|---|---|---|---|
| **W2-12** | `ml` サービスの追加手順 | **`vercel.json` に `services` と `rewrites` を足す PR と、`/ml/compact` を実装する PR を分ける。前者は `/ml/health` だけを含む** | `vercel.json` の変更は本番デプロイであり、**収集の Cron 設定の更新**でもある。現在の `rewrites` は `/(.*)` を web に流す 1 本だけで、ここに手を入れる。**W2 で収集を止めうる変更はここだけ**なので、他の変更と混ぜない。W1 の PR 0 と同じ考え方（最大の未知を最小の投資で先に潰す） |
| **W2-13** | `/v1` の DB アクセス | **匿名ロール用の読み取りビューを作り、`SUPABASE_PUBLISHABLE_KEY` で読む**（サービスロールを使わない） | 現状、匿名ロールは SELECT 権限を 1 つも持たず、RLS ポリシーも 0 件。CLAUDE.md §5 は「匿名ロールには読み取り用ビューのみ」と定めている。公開 API がサービスロールで読むと、バグ 1 つで全テーブルに手が届く。**読み取り専用のビューに限定すれば、事故の範囲がビューの中に閉じる** |

## 4. 実装で踏みそうな落とし穴

W1 で 46 件の所見が出た。そのうち「調べれば先に分かったもの」を減らすため、W2 で怪しい点を先に列挙する。実測で確かめたものには「実測」と書いた。

| # | 落とし穴 | 対処 |
|---|---|---|
| 1 | **`vercel.json` の `rewrites` は順序が意味を持つ。** 現在は `/(.*)` → `web` の 1 本だけで、`/ml/(.*)` → `ml` を**その前に**置かないと `/ml/*` が web に流れて 404 になる | PR C で `/ml/(.*)` を先頭に置く。順序を単体テストで固定する（`vercel-crons.test.ts` と同じ手口） |
| 2 | **`ml` サービスの追加は収集を止めうる。** デプロイ設定の構造変更で、失敗すれば `/api/jobs/collect/*` も落ちる | PR C をプレビューデプロイで先に確認し、マージ後は `feed_fetch_log` が毎分刻みで増えることを 5 分見る。壊れたら `vercel.json` を戻して再デプロイ（W1 の PR E2 と同じ巻き戻し） |
| 3 | **`ensure_snapshot_partitions` は今月から先しか作らない**（実測）。過去月を再構築すると DEFAULT パーティションに落ち、`monitor_feeds` が通知を上げる | PR B は取り込み前に対象月のパーティションの存在を確かめ、無ければ止まる。作成の手段は W2 では作らず、必要になった時点で判断する（§8） |
| 4 | **全面再構築で `idx` が変わり得る。** 採番は「その時点で未登録のポートを入力順に末尾へ足す」なので、流す順序が違えば別の値になる | 再構築は**必ず古い順に流す**。Parquet は `station_id` を持つ（W2-10）。既存の `status_snapshots` がある状態で過去を流しても、既に登録済みのポートには新しい `idx` を振らないので影響しない |
| 5 | **Open-Meteo の無料枠は非商用限定**（開発プラン D-20・R11）。W2 は開発中なので使えるが、商用化と同時に有料プランへ切り替える | アーカイブのパスとコードをプラン非依存にしておく（エンドポイントを定数にする）。切替時に取得先だけ差し替える |
| 6 | **座標は格子に丸められて返る**（実測：35.681 → 35.7、139.767 → 139.75）。要求した座標と応答の座標が違う | 応答に含まれる `latitude` / `longitude` を正としてアーカイブに残す。ポートとの対応づけは W4 で、**保存済みの座標**を使って行う |
| 7 | **Vercel の Python ランタイムは Node と別物。** 依存の解決・起動時間・メモリの制約が違う（開発プラン R15） | PR C は `/ml/health` だけにして、ランタイムの疎通そのものを確かめる。`pyarrow` や `polars` を入れるのは PR D で、そこで初めてサイズと起動時間を測る |
| 8 | **Parquet の書き込み先は Supabase Storage の S3 互換 API**。`.env` の `S3_PROTOCOL_*` は**ローカルスタックのもの**で、本番の資格情報は別に要る | PR D の前に本番の S3 資格情報を用意する。無ければ Storage の REST API（`/storage/v1/object`）で PUT する。どちらでも書けるが、資格情報の置き場所を先に決める |
| 9 | **毎時ジョブの「前の 1 時間」の境界。** `observed_at` で切ると、ちょうど境界の観測がどちらの時間に入るか曖昧になる | 半開区間 `[hour, hour+1)` に統一し、Parquet のパスは区間の開始時刻で決める。冪等（同じ時間は上書き）にすれば、二重起動でも結果が変わらない |
| 10 | **ビューの権限は「誰が所有するか」で決まる。** `security_invoker` の既定は `false`＝**ビューの所有者の権限と RLS が適用される**。所有者が `postgres` なら RLS を迂回して全行が見える。意図どおりだが、暗黙に頼ると危ない | 既定に頼らず `security_invoker = false` を明示し、**ビューの `where` 句で出す範囲を決める**（`geo_suspect` を除く等）。基底テーブルの権限は匿名に与えない。pgTAP で「匿名はビューを読めるが基底テーブルは読めない」を固定する |
| 11 | **`/v1/stations` の bbox は無制限に広げられる。** 全 20,742 ポートを 1 回で返す要求が来ると、応答が数 MB になる | bbox の面積に上限を設け、超えたら 400 を返す。上限は「東京 23 区がぎりぎり入る」程度から始め、実測で調整する |
| 12 | **天気アーカイブのバケットを新設すると、`supabase db diff` は検出しない**（W1 プラン §4.3 の 17） | マイグレーションに手書きで追加する。0001 と同じ形（`storage` スキーマの存在で分岐） |
| 13 | **EDA を早く回すとデータが足りない。** 9/8 時点で 2 日分、9/12 でも 6 日分しかない | EDA は W2 の**最後**に置く。曜日の効果は見ない（土日が 1 回ずつしかない）。見るのは分布・欠損・日内変動まで |

## 5. PR の分割と各 PR の定義

### 5.1 分割の方針

W1 と同じ。1 つの PR は **1 つの関心事**に閉じ、レビューが 20〜60 分で終わる量にする。各 PR はそれ自体で CI を通し、テストを含む。**本番に影響する変更を後ろに寄せる。ただし損失が日ごとに積み上がるものは最初に置く**（W1 の PR 0 と同じ例外）。

| 段 | PR | 名前 | 主な成果物 | 本番影響 | 目安 |
|---|---|---|---|---|---|
| **0** | — | 監視の通知先 | Vault に `alert_webhook_url`。強制発火で到達確認 | なし（設定のみ） | 15 分 |
| **1** | **A** | 天気予報の生アーカイブ | `/api/jobs/archive-weather`、毎時 Cron、`weather-raw` バケット | 小（Storage に書くだけ） | 半日 |
| **2** | **B** | スナップショット再構築 | `scripts/rebuild-snapshots.ts`、`tsx`、PR 0 期の取り込み | 中（過去分を DB に足す） | 半日 |
| **3** | **C** | `ml` サービスの雛形 | `apps/ml`（FastAPI・`/ml/health`）、`vercel.json`、Python の CI | **大**（デプロイ構造の変更） | 半日 |
| **4** | **D** | 毎時 Parquet 化 | `/ml/compact`、毎時 Cron、Storage の `gbfs-parquet` | 中 | 半日 |
| **5** | **E** | `/v1` の現在値 | 読み取りビュー＋RLS、`/v1/meta` の DB 接続、`/v1/stations` | 小（読み取りのみ） | 半日 |
| **6** | **F** | データ辞書 | `docs/data_dictionary.md` | なし | 2〜3 時間 |
| **7** | **G** | EDA #1 | `apps/ml` の分析スクリプトと結果の記録 | なし | 半日 |
| **8** | — | iOS の雛形 | Xcode プロジェクト、地図に現在値 | なし | 1 日（余れば） |

**この順にする理由**：段 0 と段 1 は**遅れた分がそのまま損失になる**。段 2 は保険で、段 3・4 の前提でもある（Parquet は Postgres から作るので、Postgres を作り直せることが土台になる）。段 3 は W2 で唯一収集を止めうるので、他と混ぜず、前の段が終わって落ち着いてから入る。段 5 以降は失敗しても作り直せる。

### 5.2 段 0：監視の通知先（PR なし）

**目的**：異常を検知しても誰にも届かない状態を終わらせる。

W1 で `send_alert` は完成しているが、Vault に `alert_webhook_url` が無い。**未設定でも `alert_state` には記録されるので壊れて見えない**。バックアップ収集器が入る W3 まで、これが唯一の早期警報になる。

```
# Discord か Slack の Incoming Webhook を作り、.env に ALERT_WEBHOOK_URL= を追記してから
./scripts/setup-vault.sh .env
```

**完了条件**

- `vault.decrypted_secrets` に `alert_webhook_url` がある（値は表示しない）
- `select public.send_alert('w2_smoke', '{"message":"W2 の疎通確認"}'::jsonb)` が true を返し、**実際に通知が届く**
- 送った後に `delete from public.alert_state where alert_key = 'w2_smoke'` で後片付けする

**注意**：Webhook URL は秘密として扱う（CLAUDE.md §5）。`.env` は gitignore されており、Vault に入れたあとは出力にもログにも出さない。

### 5.3 PR A：天気予報の生アーカイブ

**目的**：**その時刻に入手できた予報を、今日から取りこぼさずに残す。** 解釈は一切しない。

W1 の PR 0 と同じ役割の PR である。テーブルも特徴量も待たずに、失われるものを先に止める。

**変更ファイル**

```
supabase/migrations/<ts>_0015_weather_bucket.sql   # weather-raw バケット（0001 と同じ書き方）
packages/shared/src/constants.ts                   # OPEN_METEO_* の定数、格子の刻み
packages/shared/src/weather-path.ts                # アーカイブのパス規約（§9.1）
apps/web/lib/jobs/weather-fetch.ts                 # Open-Meteo への取得（URL 組み立てをここに閉じる）
apps/web/lib/jobs/archive-weather.ts               # 手順（格子の取得 → 分割 → 取得 → 保存 → 記録）
apps/web/app/api/jobs/archive-weather/route.ts     # 認証・入力検証・状態コード
vercel.json                                        # crons に 1 本追加（17 * * * *）
```

**手順**（`collect.ts` と同じ規律に従う）

1. `CRON_SECRET` を検証する
2. `station_attributes` の有効行から格子を作る（`geo_suspect` は除く。§9.1 の丸め）
3. 100 格子ずつに分けて Open-Meteo を呼ぶ（実測で 100 地点 2.0 秒 / 214 KB）
4. **応答をそのまま gzip して Storage に保存する。** パースも変換もしない
5. `job_runs`（`archive_weather`）に格子数・分割数・バイト数・所要を記録する

**取得の内容**

| 項目 | 値 | 理由 |
|---|---|---|
| エンドポイント | `https://api.open-meteo.com/v1/forecast` | ライブの予報。**これがアーカイブの本体** |
| モデル | `jma_msm` | 気象庁 MSM（約 5 km・1 時間値）。開発プラン §6.4 |
| 変数 | `precipitation, precipitation_probability, temperature_2m, wind_speed_10m, weather_code` | §6.3 の天気特徴量に対応。後から足せないので最初から取る |
| 予報期間 | `forecast_days=2`（48 時間） | 短期モデルの水平は 3 時間だが、長期プロファイル（168 時間）と「今日これから降るか」の表示に効く。48 時間で 1 回 214 KB / 100 地点 |
| 時刻 | `timezone=Asia/Tokyo` | 人が読むときに迷わない。保存するのは応答そのままなので解釈は W4 |
| 頻度 | 毎時（`17 * * * *`） | jma_msm は 3 時間毎に更新され、配信までに約 2 時間半かかる。毎時取れば「その時刻に入手できた最新」が必ず残る。分を 17 にしたのは、Parquet 化（`7 * * * *`）と時刻の先頭を避けて、同時に重い処理が走らないようにするため |

**なぜ「その時刻の予報」を保存するのか**：学習では `t` 時点で入手できた予報を使う（開発プラン §6.2 のリーク防止）。過去 API の既定はリード 0〜24 時間の値で、水平 3 時間の学習には新しすぎて漏洩し得る。**実測で、最新の予報と 1 日前の予報は降雨フラグが 15% 食い違う**（§10.2）。この差は無視できない。

**過去分の扱い**：9/6〜9/7 の分は Historical Forecast API で埋める。既定と `previous_day1` のどちらを使うかは W4 で決め、**使った近似を `docs/data_dictionary.md` に必ず書く**。ライブのアーカイブが始まった時刻以降は近似を使わない。

**テスト**

- 単体：格子の丸め（`35.681 → 35.7` / `139.767 → 139.75`）、分割（595 → 100 × 6）、パスの組み立て、応答が配列でない場合（1 地点だけのとき Open-Meteo は配列を返さない）
- ルート：認証・状態コード・`maxDuration` の定数との一致（`collect` と同じ手口）
- 契約：`vercel.json` の Cron が共有定数と一致することを `vercel-crons.test.ts` に追加

**完了条件**

- 本番で 2 回動き、`weather-raw` に 2 時刻ぶんのオブジェクトが揃う
- 1 回の所要が 30 秒未満（実測 100 地点 2.0 秒 × 6 ＝ 12 秒の見込み）
- `job_runs` に成功が記録され、収集の毎分刻みが乱れていない

**巻き戻し**：`vercel.json` から Cron の 1 行を消して再デプロイする。収集の Cron には触れない。

**ローカルでの実行結果（実データ、2026-09-07）**：120 格子（2 分割）で通した。

| 項目 | 実測 |
|---|---|
| 所要 | **2.1 秒**（2 分割）。595 格子なら 6 分割で約 6 秒の見込み |
| 受信バイト | 456,711 B（2 モデル）／294,067 B（1 モデルのとき） |
| gzip 後 | **31,863 B**（圧縮比 14:1） |
| 595 格子での 1 日の見込み | 約 **3.8 MB/日**（GBFS の 69 MB/日に対して十分小さい） |
| 冪等性 | 同じ時間の 2 回目は `n_saved 0 / n_duplicate 2` で成功 |
| 保存された中身 | 2 モデル × 5 変数 × 48 時間。`precipitation_probability_best_match` は 48/48 が非 null、`_jma_msm` は 0/48 |

**モデルを 2 つ取る理由**：`jma_msm` は**降水確率を返さない**（実測で全件 null）。`best_match` だけが返す。決定論的な値は日本では両者一致する。後から遡って足せない入力なので、両方を保存して W4 で選ぶ（§12 の 47）。

**本番での初回実行（2026-09-08 00:17 JST）**：**成功**。

| 項目 | 実測 |
|---|---|
| 格子 / 分割 | **595 / 6**（失敗 0） |
| 所要 | **3.94 秒**（見込みの 6 秒より速い） |
| 受信 → gzip 後 | 2,278,941 B → **185,200 B**（12.3:1） |
| 1 時間あたり | 181 KB → **約 4.3 MB/日** |
| 保存された中身 | 1 分割 95 地点 × 10 系列 × 48 時刻。`precipitation_probability_best_match` は 48/48 非 null、`_jma_msm` は 0/48 |
| 収集への影響 | 無し（毎分の刻みが乱れていない） |



### 5.4 PR B：スナップショット再構築

**目的**：**生 JSON から `status_snapshots` を作り直せることを、実データで証明する。** CLAUDE.md §6 が求めている「常に動く再構築スクリプト」を用意し、PR 0 期の積み残しを取り込む。

**変更ファイル**

```
package.json                                # tsx を devDependency に追加（W2-06）
scripts/rebuild-snapshots.ts                # 生 JSON → ingest_snapshot
scripts/rebuild-snapshots.test.ts           # 純粋部分（対象の列挙・順序）の単体テスト
```

**設計**

| 項目 | 内容 |
|---|---|
| 入力 | `gbfs-raw` の `{system}/{YYYY}/{MM}/{DD}/station_status_{epoch}.json.gz` |
| 引数 | 環境ファイル、システム、開始日、終了日、`--dry-run` |
| 正規化 | **`packages/gbfs-core` をそのまま使う**（W2-05）。本番の収集と同じ関数を通す |
| 書き込み | `ingest_snapshot` **だけ**を呼ぶ（W2-07）。`begin_fetch` / `finish_fetch` は呼ばない |
| 順序 | **必ず古い順**（W2-08 と §4 の 4）。`idx` の採番順を本番と同じにする |
| 冪等性 | `ingest_snapshot` が `(system_id, observed_at)` の重複を `duplicate` として弾く。**何度流しても増えない** |
| 事前確認 | 対象月のパーティションが在ることを確かめ、無ければ**止まる**（DEFAULT に落とさない） |
| `fetched_at` | Storage オブジェクトの `created_at` を使う（応答の受信時刻は復元できない） |
| `raw_path` | オブジェクトのパスをそのまま |

**復元できないもの**：`feed_fetch_log` の ETag・所要時間・レート制限の残り。取得そのものの記録であって、スナップショットの内容ではないので再構築の対象にしない。

**完了条件**

- `--dry-run` で対象が 158 件と表示される（ドコモ 124・HELLO 34）
- 本番に取り込み、`status_snapshots` が **158 行増える**
- 取り込んだ範囲について `reconcile-snapshot.mjs` が各システム 3 件で不一致 0（§7.3）
- **2 回目の実行で 0 行増える**（全部 `duplicate`）
- 収集が中断していない

**この PR が本番に書く理由**：スクリプトを書くだけなら本番影響は無いが、**動かさなければ「動く状態」を保証したことにならない**。CLAUDE.md §6 の要求は「常に動く状態に保つ」であって「書いてある」ではない。PR 0 期という実データで通しておけば、本当の障害時に初めて動かす事態を避けられる。

**本番での実行結果（2026-09-08）**：**合格**。

| 項目 | HELLO | ドコモ |
|---|---|---|
| Storage の対象日 | 206 オブジェクト | 718 オブジェクト |
| 取り込み済み（落とさない） | 172 | 594 |
| **再構築の対象** | **34** | **124** |
| 取り込み結果 | 新規 34 / 失敗 0 | 新規 122 ＋ 再実行 2 / 失敗 0 |
| 範囲 | 06:51:33〜09:36:32 UTC | 06:53:58〜09:39:47 UTC |

**34 + 124 = 158** で、§2 で数えた PR 0 期の積み残しと完全に一致した。

**ロックが 2 件出た。** 毎分の収集と同時に走り、`ingest_snapshot` のアドバイザリロック（クラス 8421）に弾かれた。**設計どおりの挙動**で、再実行して 2 件とも取り込めた。再構築を収集と並行して安全に流せることが実データで確認できた。

**冪等性を 2 通りで確認**：普通に再実行すると対象 0 件（既取り込みを落とさない）。`--force` で 206 件すべて落とし直すと **新規 0 / 重複 206**（RPC 側の重複排除が効く）。

**照合（§7.3）**：再構築した範囲から各システム 3 件、計 6 件で**不一致 0**。

| システム | 観測時刻（UTC） | 一致 | 現れた | 不在 |
|---|---|---|---|---|
| HELLO | 06:51:33 / 08:11:32 / 09:36:32 | 14,922 | 14,835 | 87 |
| ドコモ | 06:53:58 / 08:15:45 / 09:39:47 | 5,820 | 5,797〜5,801 | 19〜23 |

**副作用として配列長の逆転が起きた**（§12 の 51）。実害は無いが、開発プラン §6.7 の記述を訂正した。


### 5.5 PR C：`ml` サービスの雛形

**目的**：**Vercel Services に Python サービスを足したとき、収集が止まらないことを確かめる。** これ以外は何もしない。

W2 で唯一、収集を止めうる変更である。W1 の PR 0 で「Cron が Deployment Protection を越えるか」を最小の投資で先に確かめたのと同じ考え方で、**最大の未知だけを切り出す**。

**変更ファイル**

```
apps/ml/pyproject.toml            # uv、ruff、mypy --strict、pytest（CLAUDE.md §3）
apps/ml/bikechance_ml/__init__.py
apps/ml/bikechance_ml/main.py     # FastAPI。/ml/health だけ
apps/ml/tests/test_health.py
vercel.json                       # services に ml を追加、rewrites に /ml/(.*) を先頭で追加
.github/workflows/python.yml      # ruff / mypy / pytest
apps/web/vercel-rewrites.test.ts  # rewrites の順序を固定する
```

**`vercel.json` の変更**：`rewrites` は**順序が意味を持つ**（§4 の 1）。`/ml/(.*)` を `/(.*)` より**前**に置く。

```json
"rewrites": [
  { "source": "/ml/(.*)", "destination": { "service": "ml" } },
  { "source": "/(.*)",    "destination": { "service": "web" } }
]
```

**進め方**（この PR だけは慎重に）

1. プレビューデプロイで `/ml/health` が 200 を返し、`/v1/meta` と `/api/jobs/collect/*` が壊れていないことを確かめる
2. マージ後、**5 分間 `feed_fetch_log` を見る**。毎分刻みで増え続けていることを確認する
3. 壊れていたら `vercel.json` を戻して再デプロイ（PR E2 と同じ巻き戻し）

**完了条件**

- `/ml/health` が本番で 200
- **収集の呼び出し間隔の最大が 180 秒未満**（毎分が維持されている）
- `/v1/meta` が 200 のまま
- Python の CI（ruff / mypy --strict / pytest）が通る
- ビルド出力で web のルートが従来どおり

**この PR に入れないもの**：`pyarrow`・`polars`・Supabase クライアント。依存を増やすと、失敗したときに「ランタイムの問題か依存の問題か」が切り分けられない。**疎通だけを見る。**

**確認できたこと（2026-09-08、プレビューデプロイと ASGI 直叩き）**

| 確かめたこと | 結果 |
|---|---|
| `vercel.json` のスキーマに `fastapi` があるか | **ある**（framework の候補 74 件のうちの 1 つ） |
| rewrite が URL を書き換えるか | **書き換えない**（スキーマの `path` の説明）。接頭辞はアプリが持つ |
| Vercel が Python サービスをビルドできるか | **できた**（プレビューのデプロイが成功） |
| `main.py` が ASGI アプリとして成立しているか | **している**（生の ASGI で叩き、`/ml/health` が 200） |
| ml サービスが他のパスを奪っていないか | **奪っていない**（`/health` と `/v1/meta` は 404） |
| rewrites の順序 | 逆順にすると単体テストが落ちることを確認 |
| Python の CI | ruff / ruff format / mypy --strict / pytest が 11 秒で通過 |

**プレビューでは `/ml/health` を直接叩けない。** Deployment Protection が 302 を返すため（CLAUDE.md §5 のとおりで、これ自体は正しい挙動）。したがって「Vercel の経路が実際に ml サービスへ届くか」だけは本番でしか確かめられない。

**それでも危険は小さい。** ビルドが通っている以上、失敗しても `/ml/health` が 404 になるだけで、依存する処理はまだ無い。逆にビルドが失敗していれば新しいデプロイが作られず、**既存のデプロイで収集は動き続ける**。収集が止まる経路は「rewrites の書き換えを誤る」ことだけで、そこは単体テストで固定してある。


### 5.6 PR D：毎時 Parquet 化（M2）

**目的**：Postgres の 60 日保持から独立した、学習用のアーカイブを作り始める。

**変更ファイル**

```
supabase/migrations/<ts>_0016_parquet_bucket.sql   # gbfs-parquet バケット
apps/ml/bikechance_ml/jobs/compact.py              # 前 1 時間 → Parquet
apps/ml/bikechance_ml/storage.py                   # Storage への読み書き（S3 互換 or REST）
apps/ml/bikechance_ml/db.py                        # Postgres からの読み出し
apps/ml/bikechance_ml/main.py                      # /ml/compact を追加
apps/ml/tests/test_compact.py
vercel.json                                        # crons に 1 本追加（7 * * * *）
```

**Parquet のスキーマ**（§9.2 に契約として置く）

| 列 | 型 | 備考 |
|---|---|---|
| `system_id` | string | パーティション列にはしない（1 ファイルに両システムを入れない） |
| `station_id` | string | **`idx` は書かない**（W2-10） |
| `observed_at` | timestamp[ms, UTC] | フィードの `last_updated` |
| `bikes` / `docks` / `flags` / `reported_age_s` | int16 | **`-1` は「観測されなかった」。0 と区別する**（開発プラン §6.7） |

- ソート：`station_id, observed_at`。圧縮：zstd
- パス：`gbfs-parquet/{system}/date=YYYY-MM-DD/hour=HH/part.parquet`（UTC）
- 区間：半開 `[hour, hour+1)`（§4 の 9）
- **配列長より外側の `idx` は行にしない**（当時まだ台帳に無いポート。行を作ると「観測されなかった」と区別できなくなる）

**冪等性**：同じ時間帯を 2 回処理したら同じ内容を上書きする。`job_runs` に行数とバイト数を記録する。

**完了条件**

- 本番で 2 回動き、同じ時間帯の 2 回目が**行の集合として同一**
- 1 時間分の行数が期待どおり（HELLO 12 × 14,922 ＋ ドコモ 45 × 5,820 ＝ 約 44 万行）
- 1 日分の合計が **20 MB 以下**（見込み 5〜10 MB）
- 1 回の所要が 60 秒未満、Active CPU が 5 秒未満
- 収集が中断していない

### 5.7 PR E：`/v1` の現在値

**目的**：iOS と Web が叩ける最初の読み出し経路を開く。予測はまだ無いので**現在値だけ**を返す。

**変更ファイル**

```
supabase/migrations/<ts>_0017_public_views.sql   # 読み取りビュー＋匿名への grant
supabase/tests/0009_public_views.sql             # 匿名が基底テーブルを読めないことを固定する
packages/shared/src/api.ts                       # /v1/stations の応答スキーマ
apps/web/lib/api/stations.ts                     # bbox の検証と問い合わせ
apps/web/app/v1/stations/route.ts
apps/web/app/v1/meta/route.ts                    # feed_state に接続（stale を実装する）
```

**読み取りビュー**（W2-13）

```sql
create view public.v1_stations_current with (security_invoker = false) as
select l.system_id, l.station_id, a.name, a.lat, a.lon, a.capacity,
       l.bikes, l.docks, l.flags, l.is_present, l.last_changed_at, f.last_observed_at
  from public.station_status_latest l
  join public.station_attributes a
    on a.system_id = l.system_id and a.station_id = l.station_id and a.valid_to is null
  join public.feed_state f on f.system_id = l.system_id
 where not a.geo_suspect;         -- 壊れた座標は地図に出さない（開発プラン §14）
```

匿名には**このビューにだけ** SELECT を与える。基底テーブルの権限は与えない。

**`/v1/stations` の仕様**

| 項目 | 内容 |
|---|---|
| パラメータ | `bbox=west,south,east,north`（必須）、`system`（任意） |
| 上限 | bbox の面積に上限。超えたら 400（§4 の 11）。初期値は緯度 0.5° × 経度 0.5°（東京 23 区が入る） |
| 応答 | ポート配列（id・名称・座標・容量・bikes・docks・貸出可・返却可・観測時刻）、`stale`、`attribution` |
| 鮮度 | `feed_state.last_observed_at` が期待周期の 3 倍を超えたら `stale: true`。**ttl 超過の値を「現在値」として出さない**（CLAUDE.md §2 の 8） |
| キャッシュ | `public, s-maxage=60, stale-while-revalidate=120`。bbox はタイル境界に量子化する |
| 予測 | **返さない**。`p_bike` / `p_dock` は W4 |

**`/v1/meta`**：`feed_state` から `data_updated_at` を入れ、`stale` を実際に計算する。いまは `stale: true` の固定値を返している。

**完了条件**

- pgTAP で「匿名はビューを読めるが `station_status_latest` は読めない」が固定される
- 本番で `/v1/stations?bbox=...` が 200 を返し、件数が bbox 内のポート数と一致する
- `/v1/meta` の `data_updated_at` が実際の観測時刻を返す
- p75 が 500 ms 未満（CDN 未ヒット時）
- `X-Data-Attribution` ヘッダとクレジットが出る（CC BY 4.0。CLAUDE.md §2 の 8）

### 5.8 PR F：データ辞書

**目的**：**ML 側が参照する唯一の仕様書**を作る。W1 の手順 9 の持ち越し。

**変更ファイル**：`docs/data_dictionary.md`

**載せる内容**

| 節 | 内容 |
|---|---|
| テーブルと列 | 全 14 テーブルの列・型・意味・書き手（W1 プランの §11 と `comment on column` を統合する） |
| 配列の規約 | `arr[idx+1]`、`-1` の意味、`idx ≥ array_length` の意味、**ゼロ埋め禁止**（開発プラン §6.7） |
| センチネルと丸め | HELLO の返却枠 `-1` を 0 に丸めていること、`reported_age_s` の範囲、ドコモの `reported_age_s` が恒常的に 0 であること |
| システム別の差 | `station_information` の実在フィールド表（開発プラン §6.7）。`is_charging_station` は HELLO 専用、`region_id` はドコモ専用 |
| 時刻 | すべて timestamptz（UTC 保存）。`observed_at`（フィードの last_updated）と `fetched_at`（取得時刻）と `last_changed_at`（値が変わった時刻）の違い |
| アーカイブ | 生 JSON・天気・Parquet のパス規約（§9） |
| 天気の近似 | 過去分にどの発行時点の予報を使ったか（**W4 で確定するまで「未確定」と明記する**） |

**完了条件**：ML の実装者（＝自分）が、この 1 枚だけを見て特徴量パイプラインの入力仕様を決められる。

### 5.9 PR G：EDA #1

**目的**：分布を把握し、モデル設計の前提が実データと合っているかを見る。**W2 の最後に置く**（§4 の 13）。

**変更ファイル**：`apps/ml/bikechance_ml/analysis/eda_01.py` と、結果を記録する `docs/260912_eda_01.md`

**見るもの**

| 観点 | 何を見るか | なぜ |
|---|---|---|
| 分布 | `bikes` / `docks` のゼロ率、容量に対する充足率 | 「借りられない・返せない」がどれくらい起きているか。アプリの価値が出る領域の広さ |
| 日内変動 | 時間帯 × ゼロ率、時間帯 × 変化量 | 特徴量の `minute_of_day` が効くかの当たり |
| ポート差 | ゼロ率のポート別分布、変化の多いポート／動かないポート | ポート別プロファイルの必要性 |
| 欠損 | `-1` の割合、`idx ≥ array_length` の割合、フィードの欠損区間 | 除外規則（開発プラン §6.1）の実効範囲 |
| 流量 | 本来の周期と 5 分グリッドでの `|Δbikes|` の差 | W1 で 13% と測った差が、日数を増やしても保たれるか |
| 再配置 | `|Δbikes| ≥ max(4, 0.5 × capacity)` の件数と時刻分布 | 開発プラン §6.5 の検知規則が妥当か |

**完了条件**：上の 6 つについて図か表があり、開発プランの前提と食い違う点が列挙されている。**食い違いが見つかったら開発プランを直す**（W1 と同じ運用）。

### 5.10 段 8：iOS の雛形（余力があれば）

Xcode プロジェクトを作り、`/v1/stations` を叩いて地図にポートを出すところまで。**W2 の必須ではない**。段 1〜7 が終わっていなければ W3 に送る。理由は、iOS は失敗しても失われるものが無く、他の 7 つは違うからである。

## 6. スケジュール（ゲート方式）

日付は目標であり、**各段のゲートを満たすまで次に進まない**。W1 と同じ運用にする。

| 段 | 目標日 | 内容 | ゲート（次に進む条件） |
|---|---|---|---|
| 0 | 9/8（月）午前 | 監視の通知先 | 強制発火が**実際に届く** |
| 1 | 9/8（月） | **PR A** 天気予報の生アーカイブ | 2 時刻ぶんが Storage に貯まる。**ここが最重要ゲート** |
| 2 | 9/9（火）午前 | **PR B** 再構築 | PR 0 期 158 件を取り込み、照合の不一致 0。2 回目で 0 行増える |
| 3 | 9/9（火）午後 | **PR C** `ml` サービスの雛形 | `/ml/health` が 200 で、**収集が毎分のまま** |
| 4 | 9/10（水） | **PR D** 毎時 Parquet 化 | **M2**：2 回実行で同一。1 日の見込みが 20 MB 以下 |
| 5 | 9/11（木） | **PR E** `/v1` の現在値 | 匿名の権限が pgTAP で固定され、bbox の応答が正しい |
| 6 | 9/11（木）午後 | **PR F** データ辞書 | 入力仕様がこの 1 枚で決まる |
| 7 | 9/12（金） | **PR G** EDA #1 | 6 つの観点に図か表がある |
| 8 | 9/12（金） | iOS の雛形（余力があれば） | 地図に現在値が出る |

**段 1 を最重要ゲートにする理由**：他の 7 つは、遅れても後から取り返せる。天気だけは違う。1 日遅れれば 1 日分の「その時刻に入手できた予報」が永久に失われる。実装量は半日で、リスクは Storage に書くだけである。**最初に置かない理由が無い。**

**段 3 の位置**：収集を止めうる唯一の変更なので、段 1・2 が落ち着いてから入る。逆に、段 4 以降は段 3 が通らないと始まらないので、後ろに寄せすぎてもいけない。火曜の午後がちょうどよい。

**前倒しする場合**：段 5・6・7 は互いに独立で、並行して進められる。段 0〜4 の順序は動かさない。

**遅れた場合**：段 8（iOS）を W3 に送る。それでも足りなければ段 7（EDA）を送る。**段 0〜4 は送らない。** 天気は損失が積み上がり、再構築と Parquet は 11/30 の期限があるため。

## 7. 検証の手順と合格判定

### 7.1 収集が止まっていないこと（毎段で確認）

W2 の全ての PR で、マージ後にこれを確認する。W1 で作った判定スクリプトがそのまま使える。

```
psql "$SUPABASE_DB_URL" -f scripts/verify-m1.sql
```

見るのは 3 行だけでよい。**#11 収集の呼び出し間隔の最大**（180 秒未満）、**#8 失敗率**（1% 未満）、**#4 重複行**（0）。他の項目は W1 で合格済みで、W2 で壊すとしたらこの 3 つに出る。

### 7.2 天気アーカイブ

```sql
-- 直近 24 時間の取得回数（期待 24、合格 23 以上）
select count(*) from storage.objects
 where bucket_id = 'weather-raw' and created_at > now() - interval '24 hours';

-- 時刻ごとの分割数（期待 6：595 格子 ÷ 100）
select date_trunc('hour', created_at) as h, count(*)
  from storage.objects where bucket_id = 'weather-raw'
 group by 1 order by 1 desc limit 5;
```

**合格ライン**：24 時間で 23 回以上、1 回あたり 6 オブジェクト、`job_runs` の `archive_weather` に失敗が無い。

### 7.3 再構築の一致

取り込んだ範囲について、W1 の照合スクリプトをそのまま使う。**再構築した行を、元の生 JSON と突き合わせる**という意味で、これが最も強い検証になる。

```
node scripts/reconcile-snapshot.mjs .env hellocycling <再構築した observed_at>
node scripts/reconcile-snapshot.mjs .env docomo-cycle  <再構築した observed_at>
```

**合格ライン**：各システム 3 件で不一致 0。加えて、再構築を 2 回目に流したとき `status_snapshots` が 1 行も増えないこと（冪等）。

### 7.4 Parquet

```sql
-- 1 日分の合計サイズ（見込み 5〜10 MB、合格 20 MB 以下）
select sum((metadata->>'size')::bigint) from storage.objects
 where bucket_id = 'gbfs-parquet' and name like '%date=2026-09-10%';
```

冪等性は、同じ時間帯を 2 回処理して行の集合が一致することで見る。Parquet はメタデータに書き込み時刻が入るのでバイト単位では一致しないことがある。**行の集合で判定する。**

**合格ライン**：1 時間分が約 44 万行、1 日 20 MB 以下、2 回目の実行で行の集合が同一。

### 7.5 `/v1` の権限

pgTAP で固定する。**匿名がビュー越しにしか読めないこと**が要点で、ここが緩むと DB 直結を禁じている意味が無くなる。

```sql
select ok(has_table_privilege('anon', 'public.v1_stations_current', 'select'), 'ビューは読める');
select ok(not has_table_privilege('anon', 'public.station_status_latest', 'select'), '基底テーブルは読めない');
select ok(not has_table_privilege('anon', 'public.status_snapshots', 'select'), 'スナップショットも読めない');
```

### 7.6 週末の総点検

金曜に W1 の M1 判定をもう一度流し、W2 で何も壊れていないことを確かめる。

```
psql "$SUPABASE_DB_URL" -v from_at='<24 時間前>' -v baseline_bytes=<開始時の値> -f scripts/verify-m1.sql
```

**合格ライン**：11 項目すべて W1 と同じ結果。DB の日次増分が 40 MB を超えていない。

## 8. W2 では作らないもの

範囲を絞るために、次は意図的に W2 の外に置く。

| 項目 | 実施週 | 理由 |
|---|---|---|
| 天気のテーブル化・特徴量 | W4 | W2 は保全だけ。解釈を待つと保全が遅れる（W2-01） |
| 過去分の天気バックフィル | W4 | どの発行時点を使うかの判断が要る。ライブのアーカイブが先 |
| 過去月のパーティション作成 | 必要になった時点 | W2 で扱う 9 月は現行月。**本当の障害復旧では必ず要る**ので、`ensure_snapshot_partitions` の制約を §4 の 3 に記録した |
| `/v1/stations` の予測・グリッド集約 | W4 / W5 | 予測テーブルがまだ無い。低ズームの集約は実測してから決める |
| 近傍リスト・履歴プロファイル | W3 | 特徴量の作業 |
| 祝日・行政区域の取り込み | W3 | 同上 |
| バックアップ収集器（Edge Function） | W3 | まず単一系＋ウォッチドッグの欠損率を測ってから（開発プラン §5.7）。W1 の実測は欠損 0 |
| Drive ミラー | W3 以降 | Parquet が貯まってから |
| 生 JSON の Drive 移管・Storage からの削除 | 12 か月後 | Parquet で完全再現できると確認できた期間のみ（開発プラン §5.6） |
| モデル・予測に関するテーブル | W4 | 空テーブルを先に作らない（W1-10 と同じ方針） |

## 9. 付録：PR をまたぐ契約

### 9.1 天気アーカイブのパス規約

```
weather-raw/{YYYY}/{MM}/{DD}/jma_msm_{hour_epoch_s}_{batch:02d}.json.gz
例: weather-raw/2026/09/08/jma_msm_1788800400_00.json.gz
```

- 日付は **UTC**（`gbfs-raw` と同じ。W1 プラン §11.5）
- `hour_epoch_s` は**取得を開始した時刻を「時」に丸めた値**（§12 の 48）。応答に発行時刻が無いため、これが「いつ入手したか」の記録になる。丸めるのは**同じ時間の再実行を同じパスに写像する**ためで、取り直しが 409 として畳まれ、1 時間 1 個という数え方も崩れない
- `batch` は分割の連番（00〜05）。応答に `latitude` / `longitude` が含まれるので、どの格子かは中身で分かる
- ファイル名の `jma_msm` は**保存先の規約としての名前**で、中身のモデルが増えても変えない
- 中身は **Open-Meteo の応答そのまま**。整形も並べ替えもしない。**2 モデル分**（`jma_msm` と `best_match`）が接尾辞つきの系列で入る（§12 の 47）

**格子の丸め**：`lat` は 0.05°、`lon` は 0.0625° 刻み（jma_msm の格子。実測で確認）。

```
gy = round(lat / 0.05),  gx = round(lon / 0.0625)
セルの代表点 = (gy * 0.05, gx * 0.0625)
```

要求した座標と応答の座標は一致しないことがある（§4 の 6）。**対応づけには応答側の座標を使う。**

### 9.2 Parquet のスキーマ

```
gbfs-parquet/{system}/date=YYYY-MM-DD/hour=HH/part.parquet     ← 日時は UTC
```

| 列 | 型 | 意味 |
|---|---|---|
| `system_id` | string | `hellocycling` / `docomo-cycle` |
| `station_id` | string | 提供側の識別子。**`idx` は書かない**（W2-10） |
| `observed_at` | timestamp[ms, UTC] | フィードの `last_updated` |
| `bikes` | int16 | 貸出可能台数。**`-1` は「このスナップショットに現れなかった」** |
| `docks` | int16 | 返却可能枠。同上。**HELLO の定員超過は 0 に丸めてある**（W1-25） |
| `flags` | int16 | 1=設置 2=貸出可 4=返却可 のビット和。同上 |
| `reported_age_s` | int16 | 観測時刻とポートの報告時刻の差。**ドコモは恒常的に 0** |

- ソート：`station_id, observed_at`。圧縮：zstd
- **配列長より外側の `idx`（当時まだ台帳に無いポート）は行にしない。** 行を作ると `-1`（登録済みだが未出現）と区別できなくなる

### 9.3 再構築の手順

```
# 1. 対象月のパーティションが在ることを確かめる
psql "$SUPABASE_DB_URL" -c "select inhrelid::regclass from pg_inherits where inhparent = 'public.status_snapshots'::regclass"

# 2. 何が対象になるかを見る（書かない）
pnpm exec tsx scripts/rebuild-snapshots.ts .env hellocycling 2026-09-06 2026-09-06 --dry-run

# 3. 取り込む
pnpm exec tsx scripts/rebuild-snapshots.ts .env hellocycling 2026-09-06 2026-09-06

# 4. 照合する
node scripts/reconcile-snapshot.mjs .env hellocycling <observed_at>
```

**守ること**

- **古い順に流す**（`idx` の採番順を本番と揃える）
- `ingest_snapshot` **だけ**を呼ぶ（`feed_state` を触らない）
- 対象月のパーティションが無ければ**止まる**（DEFAULT に落とさない）
- 何度流しても増えない（`(system_id, observed_at)` の重複は `duplicate`）

### 9.4 予測の水平と天気データの対応

「水平ごとに未来の天気を貯める必要があるのでは」という問いへの答えを残す。**必要ない。1 回の取得が全ての水平を覆う。**

**モデルが要るもの**：基準時刻 `t`、水平 `h ∈ {5, 10, …, 180}` 分に対し、`t` 時点で入手できた「`t+h` の予報」。

**1 回の取得が返すもの**：`forecast_days=2` の応答は、**その時刻に発行されている予報の系列そのもの**で、今日 00:00 JST から明日 23:00 JST までの 1 時間値が入る。つまり `t+5 分` から `t+180 分` までは全部この 1 本の中にある（3 時間先までなら高々 4 つの連続する時刻）。

したがって取り出し方はこうなる。

```
（t, h）に対する天気 = 「t 以前で最新のアーカイブ」を開き、「t+h を含む時刻」の値を読む
```

**水平ごとに別々のファイルを貯める必要は無い。** 貯めるのは「発行時点ごとの未来の系列」で、水平はその系列への添字にすぎない。

| 誤解しやすい形 | 実際 |
|---|---|
| 水平 10 点それぞれについて未来の予報を保存する | 1 つの系列に 10 点すべてが含まれる |
| 予測のたびに天気を取りに行く | 毎時 1 回のアーカイブを読むだけ |
| 予報を「その時刻の実況」として保存する | 保存するのは**発行時点の未来の見通し**。実況ではない |

**毎時の取得で足りる理由**：`jma_msm` は **3 時間ごとに更新**され、配信までに約 2 時間半かかる。毎時取れば、新しい発行を最大 1 時間の遅れで捕まえる。**モデルの更新周期より細かく取っても新しい情報は増えない。** 現状は 3 倍の余裕で取っている。

**基準時刻からの遅れ**：`t` で使えるのは「`t` 以前で最新の取得」なので、遅れは 0〜60 分。過去 API の近似（既定はリード 0〜24 時間、`previous_day1` は 24〜48 時間）に比べて桁違いに小さい。**ライブのアーカイブを始めた意味はここに出る。**

**15 分値を取らない理由（実測にもとづく）**：`minutely_15` は `jma_msm` でも返るが、**1 時間値から機械的に作られていて新しい情報が無い**。2026-09-07 の実測で、15 分の降水量は「次の 1 時間値 ÷ 4」を 15 分ずらして並べたものだった。

| 時刻 | 1 時間値 | 15 分値 × 4 |
|---|---|---|
| 00:00 | 10.40 | 2.6, 3.0, 3.0, 3.0 |
| 01:00 | 11.80 | 3.0, 4.3, 4.3, 4.3 |
| 02:00 | 17.00 | 4.3, 5.0, 5.0, 5.0 |
| 03:00 | 19.80 | 5.0, 1.3, 1.3, 1.3 |

`10.40 ÷ 4 = 2.6`、`11.80 ÷ 4 ≈ 3.0`、`17.00 ÷ 4 ≈ 4.3`。各時刻の先頭 1 つはその時刻の 1 時間値を、残り 3 つは**次の時刻**の 1 時間値を 4 で割った値である。気温も線形補間だった。**情報が増えないのに容量が 3 倍になり、しかも 15 分ずれる**ので取らない。天気の実効的な分解能は 1 時間であり、それはデータ源の性質であってこちらの取り方の問題ではない。

**W4 で間違えやすい点**

| 点 | 内容 |
|---|---|
| as-of の選び方 | `t` **以前**で最新のアーカイブを使う。`t` より後に取得したものを使うと漏洩する（水平が長いほど効く） |
| 降水量の意味 | Open-Meteo の 1 時間降水量は**直前 1 時間の合計**。`t+h` の値は「`t+h` を含む時刻」の行から読む。境界の扱いを実装前に 1 度検算する |
| モデルの選択 | 決定論的な値は `jma_msm`、降水確率は `best_match`（§12 の 47）。同じファイルに両方入っている |
| 経過した時刻 | 応答には今日の既に過ぎた時刻も含まれる。これは予報ではなく解析に近い値なので、**特徴量に混ぜない**（`precip_mm_now` に使うのは「`t` を含む時刻」までにとどめる） |

**取得窓を変えるか**：`past_hours` / `forecast_hours` を使えば「今から前後何時間」という一定の窓にできる（実測で動作を確認）。現行の `forecast_days=2` は日付境界に揃うため、先の覆いが時刻によって 23.7〜46.7 時間と変わる。**水平が 3 時間である限りどちらでも足り、変える理由が無い**ので現行のままとする。将来 12 時間以上先の天気を使う設計にするなら、そのときに `forecast_hours` へ移す。

## 10. 根拠にした一次情報（2026-09-07 実測）

本文の数値の出どころ。W2 の判断はここに載っているものだけを根拠にしている。

### 10.1 Open-Meteo の疎通と粒度

| 確かめたこと | 結果 |
|---|---|
| Historical Forecast API が `jma_msm` の過去予報を返すか | **返る**。2026-09-06 の 24 時間ぶんが `precipitation` / `temperature_2m` / `wind_speed_10m` / `weather_code` すべて非 null |
| アーカイブの反映の遅れ | **無い**。当日（2026-09-07）の 24 時間ぶんが取れる |
| 発行時点の違う予報を選べるか | **選べる**。`precipitation_previous_day1` / `_previous_day2` が使える |
| 複数地点をまとめられるか | **まとめられる**。3 地点でも 100 地点でも配列で返る |
| 100 地点 × 48 時間の 1 回の要求 | HTTP 200、**214 KB、2.04 秒** |
| 座標の丸め | 要求 `35.681, 139.767` → 応答 `35.7, 139.75`。**格子は 0.05° × 0.0625°**（jma_msm の 5 km 格子） |

**ポートが分布する格子の数**（`station_attributes` の有効行、`geo_suspect` を除く）

| システム | 格子数 |
|---|---|
| HELLO | 463 |
| ドコモ | 218 |
| **重複を除いた合計** | **595** |

1 格子あたりのポート数は中央値 6・最大 427。ポートが 1 つだけの格子が 134 個ある。
**開発プランの「約 50 クラスタ」は実態より 1 桁少ない。** まとめ取得ができるので、595 全部を 6 要求で取る（W2-03）。

### 10.2 予報の発行時点による違い（東京駅・2026-08・744 時間・jma_msm）

| 比較 | 降水量の MAE | 降雨フラグ（>0.1mm）の一致 | 雨だった時間 |
|---|---|---|---|
| 既定（直近の予報）| — | — | 95 時間（合計 127.1 mm） |
| 既定 vs **1 日前**の予報 | 0.272 mm/h | **85.1%** | 108 時間 |
| 既定 vs **2 日前**の予報 | 0.280 mm/h | 84.4% | 91 時間 |

**1 日前の予報は、最新の予報と降雨フラグが 15% 食い違う。** 学習に使う「`t` 時点で入手できた予報」は、既定（リード 0〜24 時間）より古く、1 日前より新しい。**どちらの近似も 15% 規模の誤差を持ち込む。** ライブのアーカイブだけが厳密に正しい（W2-04）。

### 10.3 W1 完了時点の蓄積（2026-09-07 23:00 JST）

| 項目 | HELLO | ドコモ |
|---|---|---|
| スナップショット | 341 件 | 1,210 件 |
| 生 status（gzip） | 375 個 / 40 MB | 1,334 個 / 47 MB |
| 生 information（gzip） | 2 個 / 1,743 kB | 2 個 / 374 kB |
| 属性の有効行 | 14,922 | 5,820 |

DB は 60 MB。ジョブ失敗 0・取得エラー 0・異常スナップショット 0。
**PR 0 期の積み残しは 158 オブジェクト**（ドコモ 124・HELLO 34、2026-09-06 06:51〜09:40 UTC）。

### 10.4 実行環境の制約

| 確かめたこと | 結果 |
|---|---|
| Node の型ストリップで `gbfs-core` を読めるか | **読めない**。`exports` が `./src/index.ts` を指し、内部の import に拡張子が無いため ESM の解決に失敗する |
| `tsx` / `vite-node` / `vite` / `esbuild` / `jiti` | **どれも未導入**。TS スクリプトの実行系が無い（W2-06） |
| `ensure_snapshot_partitions` の作成範囲 | **今月から `p_months_ahead` か月先まで**。過去月は作らない（§4 の 3） |
| 匿名ロールの SELECT 権限 | **0 テーブル**。RLS ポリシーも 0 件。RLS 自体は 14 テーブル全部で有効 |
| `station_status_latest` | HELLO 14,922 行・ドコモ 5,820 行。`is_present` は 14,921 / 5,806 |
| `vercel.json` の `rewrites` | `/(.*)` → `web` の 1 本のみ。`services` は `web` だけ |
| `/v1/meta` | 本番で 200。ただし **DB 未接続で `stale: true` を返す** |

## 11. この文書の更新方針

W1 プランと同じ運用にする。

- 実装中に設計判断が変わったら、**§3 と開発プランの該当章を先に直してから**進む（CLAUDE.md §2 の原則 10）
- 実装中に見つかった問題は §12 として追記し、番号は W1 の 46 から続けて 47 番から採る
- 各 PR の完了条件を満たしたら、本番の実測値を §5 の該当節に書き足す
- W2 が終わったら §1 の合格基準に対する結果を記録して閉じる

## 12. 実装中に見つかった問題

W1 の 46 番から続けて採番する。

| # | 重大度 | 問題 | 修正 | 反映 |
|---|---|---|---|---|
| 47 | **中** | **`jma_msm` は降水確率を返さない。** `precipitation_probability` を要求しても全件 null だった（実測 960/960 が null）。開発プラン §6.3 の `precip_prob_fcst` はこのモデルだけでは作れない。モデル未指定（`best_match`）なら値が入る。**後から遡って足せない入力なので、気づかないまま 1 か月アーカイブしていたら、その期間の降水確率は永久に失われていた** | `models=jma_msm,best_match` の 2 モデルを 1 要求で取る。応答の系列名にモデル名の接尾辞が付く（`precipitation_jma_msm` / `precipitation_best_match`）。日本では決定論的な値は両者一致し、`best_match` だけが降水確率を持つ。応答は約 1.5 倍になるが gzip 後で +27%（1 日 3.8 MB の見込み）。**迷ったら取る**方に倒した | `constants.ts`、`weather-fetch.ts` |
| 48 | 軽微 | 取得時刻をそのままパスに使うと、**同じ時間に 2 回動いたとき別のオブジェクトができる**。取り直しが重複を生み、1 時間 1 個という数え方も崩れる | パスの時刻を**「時」に丸める**（§9.1）。同じ時間の再実行は同じパスに写像され、`upsert: false` で 409 として畳まれる。実測で 2 回目は `n_saved 0 / n_duplicate 2` になった | `weather-path.ts` |
| 49 | 軽微 | 分割の失敗理由に `error_name` が入らず、`phase: message` だけだった。`LocationCountMismatch` のような**種別が記録に残らない** | `phase/error_name: message` にした。後から `job_runs` を grep できる | `archive-weather.ts` |
| 50 | 軽微 | `storage.objects` を SQL で直接 delete できない（`storage.protect_delete()` に弾かれる）。ローカルで取り直すときに詰まる | Storage の REST API（`DELETE /storage/v1/object/{bucket}/{path}`）を使う。**本番のオブジェクト削除は確認が要る操作**なので（CLAUDE.md §6）、この経路を使うのはローカルだけ | 運用メモ |
| 51 | 中 | **再構築したスナップショットの配列長は「再構築した時点の台帳」で決まる。時系列で単調に増えるとは限らない。** 実測：再構築した 15:51〜18:39 JST の行は配列長 14,922（現在の台帳）だが、当時そのまま取り込まれた 18:41 以降の行は 14,835 から始まる。**最も古い行が最も長い配列を持つ**という逆転が起きた。開発プラン §6.7 の「古いスナップショットの配列は短い」という記述は、再構築を挟むと成り立たない | 実害は無い（増えた枠は `-1` ＝「観測されなかった」で、学習の除外規則は `-1` も `idx ≥ array_length` も同じく落とす）。ただし**「古い＝短い」という前提でローダーを書くと壊れる**ので、比較は必ず**その行の `array_length`** に対して行う。開発プラン §6.7 の記述を訂正した | 開発プラン §6.7 |
| 52 | 中 | **性能テストが壁時計で測っていて、並列実行のたびに落ちるようになった。** 単独なら 50 ms のものが、3 プロジェクトを並列に走らせると 233 ms になり、実装を変えていないのに 150 ms の上限を超えた。CPU 時間に変えても 160 ms で、メモリ帯域の取り合いは避けられない。**閾値を上げれば通るが、それでは何を守っているのか分からなくなる** | `JSON.parse` だけの時間を同じ実行の中で測り、**その何倍か**で判定する。マシンの速さも負荷も両方に等しく効くので比は安定する。実測で単独 1.92 倍・並列でも通過。守りたいのは「パースの上に載せた処理が不相応に重くないか」であって、マシンの速さではない | `perf.test.ts` |
| 53 | 軽微 | `tsx` を入れるだけでは動かない。依存の `esbuild` が導入時にプラットフォーム別のバイナリを置く必要があり、pnpm は既定でビルドを保留する。またルートの `package.json` がワークスペースのパッケージに依存していないため、`scripts/` から `@bikechance/gbfs-core` を解決できない | `pnpm-workspace.yaml` の `allowBuilds` で `esbuild: true` を明示し、ルートの devDependencies に 2 つのワークスペースパッケージと `@types/node` を足す。あわせて `scripts/**/*.ts` を lint と typecheck の対象に入れた（`tsconfig.scripts.json`）。**入れただけでは CI に載らない** | `pnpm-workspace.yaml`、`package.json`、`tsconfig.scripts.json`、`eslint.config.mjs` |
| 54 | 中 | **開発プラン §12.1 の `services.ml.functions` は `vercel.json` のスキーマに無い項目だった。** スキーマ上 `services.<name>` に置けるのは `root` / `framework` / `runtime` / `entrypoint` / 各種コマンド / `bindings` だけで、`additionalProperties: false` なので**書けば検証で弾かれる**。`maxDuration` はトップレベルの `functions` に glob で指定する | PR C は `maxDuration` が要らないので入れない。実処理が入る PR D で、トップレベルの `functions` に `apps/ml/**` の形で指定する。開発プランの骨子も直す | 開発プラン §12.1 |
| 55 | 軽微 | **ruff の `RUF002` / `RUF003` は日本語の約物をすべて誤検知する**（初回で 16 件）。全角括弧はコメントと docstring を日本語で書く限り必ず出る | ルールごと切らずに、実際に使う約物だけを `allowed-confusables` で許可する。**表意文字空白（U+3000）は入れない**。目に見えない空白は本当に事故のもとなので、検出され続けてほしい | `apps/ml/pyproject.toml` |
| 56 | 軽微 | `eslint` と `prettier` が `apps/ml/.venv` の中に vendor された `.js` を拾って落ちた。`.gitignore` には入っているが、**どちらも `.gitignore` を見ない** | eslint の `ignores` と `.prettierignore` の両方に `.venv` を足す。Python のディレクトリが増えるたびに同じことが起きるので、`**/.venv/**` の形で書く | `eslint.config.mjs`、`.prettierignore` |

**実装で足したもの**（§5.3 の変更ファイル一覧に対する差分）

| ファイル | 理由 |
|---|---|
| `apps/web/lib/jobs/weather-port.ts` | 格子の取得と `job_runs` の記録を差し替え可能にする。`IngestPort` / `AttributesPort` と同じ形 |
| `apps/web/lib/jobs/storage.ts`（変更） | `createSupabaseUploader` にバケットを引数で渡せるようにした。既定は `gbfs-raw` なので収集側は変わらない |
| `.github/workflows/ci.yml`（変更） | `fetch` の閉じ込めガードに `weather-fetch.ts` を追加。**そのぶん「weather-fetch.ts にトークンが混ざらない」検査を新設**して、緩めた分を埋めた |
| `supabase/tests/0009_weather_grid.sql` | 格子の丸めと除外（`geo_suspect`・閉じた属性行）を固定する |
