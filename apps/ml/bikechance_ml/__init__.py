"""BikeChance の ML サービス。

W2 の PR C は疎通だけを担う。学習・推論・圧縮のモジュールは W2 の PR D 以降で足す
（開発プラン §12.1 の構成に従い、`datasets` / `features` / `baselines` / `train` /
`evaluate` / `infer` / `jobs` / `io` を並べる）。
"""

__all__ = ["__version__"]

__version__ = "0.0.0"
